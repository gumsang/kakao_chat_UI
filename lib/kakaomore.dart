import 'package:flutter/material.dart';

class KakaoMore extends StatelessWidget {
  const KakaoMore({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
